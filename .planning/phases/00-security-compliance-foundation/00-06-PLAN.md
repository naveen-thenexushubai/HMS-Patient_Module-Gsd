---
phase: 00-security-compliance-foundation
plan: 06
type: execute
wave: 3
depends_on: [00-03]
files_modified:
  - src/test/java/com/hospital/security/audit/AuditInterceptorTest.java
  - src/main/java/com/hospital/security/audit/AuditInterceptor.java
autonomous: true
gap_closure: true
requirements:
  - SEC-02
  - SEC-09

must_haves:
  truths:
    - "All 26 security tests pass without failures"
    - "Test infrastructure respects append-only audit log design"
    - "Audit log details field properly populated in all scenarios"
  artifacts:
    - path: "src/test/java/com/hospital/security/audit/AuditInterceptorTest.java"
      provides: "Fixed test cleanup using @DirtiesContext instead of deleteAll()"
      contains: "@DirtiesContext"
    - path: "src/main/java/com/hospital/security/audit/AuditInterceptor.java"
      provides: "Details field properly populated in test context"
      contains: "buildDetails"
  key_links:
    - from: "AuditInterceptorTest"
      to: "Spring test framework"
      via: "@DirtiesContext annotation"
      pattern: "@DirtiesContext\\(classMode"
    - from: "AuditInterceptor"
      to: "details Map"
      via: "buildDetails() method"
      pattern: "buildDetails\\(joinPoint\\)"
---

<objective>
Fix 4 failing AuditInterceptorTest cases by refactoring test cleanup to respect immutable audit log design and ensuring details field population works correctly in test scenarios.

Purpose: Close verification gaps to achieve 100% test pass rate, completing Phase 0 security foundation with full test coverage
Output: All 26 security tests passing, audit logging fully verified
</objective>

<execution_context>
@/Users/Naveen-Ainexus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Naveen-Ainexus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Naveen-Ainexus/Projects/Hospital_Gsd/.planning/PROJECT.md
@/Users/Naveen-Ainexus/Projects/Hospital_Gsd/.planning/ROADMAP.md
@/Users/Naveen-Ainexus/Projects/Hospital_Gsd/.planning/STATE.md
@/Users/Naveen-Ainexus/Projects/Hospital_Gsd/.planning/phases/00-security-compliance-foundation/00-03-SUMMARY.md
@/Users/Naveen-Ainexus/Projects/Hospital_Gsd/.planning/phases/00-security-compliance-foundation/00-VERIFICATION.md

# Source files to fix
@/Users/Naveen-Ainexus/Projects/Hospital_Gsd/src/test/java/com/hospital/security/audit/AuditInterceptorTest.java
@/Users/Naveen-Ainexus/Projects/Hospital_Gsd/src/main/java/com/hospital/security/audit/AuditInterceptor.java
@/Users/Naveen-Ainexus/Projects/Hospital_Gsd/src/main/java/com/hospital/security/audit/AuditLogRepository.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor test cleanup to use @DirtiesContext instead of deleteAll()</name>
  <files>src/test/java/com/hospital/security/audit/AuditInterceptorTest.java</files>
  <action>
Fix Gap 1 (OptimisticLockingFailureException during test cleanup):

1. **Remove all deleteAll() calls** from test methods (lines 68, 87, 103)
   - Reason: AuditLogRepository inherits from JpaRepository, which provides deleteAll(), but database has rules (audit_logs_no_update, audit_logs_no_delete) preventing deletion of immutable audit logs

2. **Add @DirtiesContext at class level** to reset Spring context between test classes:
   - Import: `import org.springframework.test.annotation.DirtiesContext;`
   - Add annotation: `@DirtiesContext(classMode = DirtiesContext.ClassMode.AFTER_CLASS)`
   - Reason: This causes Spring to create a fresh database connection and clear audit logs between test classes, respecting append-only design

3. **Update test assertions** to work with accumulated data:
   - `auditLog_capturesReadOperation` (line 45): Use `logs.stream().filter(...)` to find the relevant log instead of assuming index 0
   - `auditLog_capturesCreateOperation` (line 66): Remove deleteAll(), use stream filter to find the CREATE log
   - `auditLog_notCreatedForUnauthenticatedUser` (line 85): Count logs before and after, verify no new logs added (instead of checking isEmpty)
   - `findByResource_returnsCorrectLogs` (line 101): Remove deleteAll(), query should still return correct filtered results

4. **Alternative approach if @DirtiesContext too slow**: Replace @Transactional with manual transaction rollback
   - Remove `@Transactional` from class level
   - This would prevent commits, but requires more test refactoring
   - Use @DirtiesContext as primary solution (cleaner, respects actual database state)

Per 00-03 decision: Application-level logging satisfies HIPAA, test infrastructure must respect append-only design established in Phase 0.
  </action>
  <verify>
mvn test -Dtest=AuditInterceptorTest

Expect:
- auditLog_capturesReadOperation: PASS
- auditLog_capturesCreateOperation: PASS
- auditLog_notCreatedForUnauthenticatedUser: PASS
- findByResource_returnsCorrectLogs: PASS
- findByUserSince_returnsRecentLogs: PASS (should already pass)

No OptimisticLockingFailureException during cleanup
  </verify>
  <done>All 5 AuditInterceptorTest tests pass, no OptimisticLockingFailureException, test infrastructure respects immutable audit log design</done>
</task>

<task type="auto">
  <name>Task 2: Investigate and fix details field null issue in test context</name>
  <files>src/main/java/com/hospital/security/audit/AuditInterceptor.java</files>
  <action>
Fix Gap 2 (details field null in test scenarios):

1. **Analyze current buildDetails() implementation** (line 138-143):
   - Builds Map with "method" and "class" keys
   - Should never return null - always returns a HashMap
   - Likely issue: Map is not being persisted correctly to database in test context

2. **Check if issue is test-only or production**:
   - Review test output: Does details field exist in database query?
   - If null in database: Problem is with JSONB persistence
   - If null only in test assertions: Problem is with test data loading

3. **Potential fixes**:

   **Fix A: Ensure buildDetails() never returns null map**
   - Wrap in null safety: Check if joinPoint is null before calling getSignature()
   - Add null checks for getTarget() and getClass()
   - Return empty map if any part fails, never null

   **Fix B: If JSONB persistence issue in tests**
   - Verify @JdbcTypeCode(SqlTypes.JSON) annotation on details field in AuditLog entity
   - Ensure test database supports JSONB (PostgreSQL 16 does)
   - Check if test transactions are committing properly

   **Fix C: Make buildDetails more robust for test scenarios**
   ```java
   private Map<String, Object> buildDetails(JoinPoint joinPoint) {
       Map<String, Object> details = new HashMap<>();
       try {
           if (joinPoint != null && joinPoint.getSignature() != null) {
               details.put("method", joinPoint.getSignature().getName());
           }
           if (joinPoint != null && joinPoint.getTarget() != null) {
               details.put("class", joinPoint.getTarget().getClass().getSimpleName());
           }
       } catch (Exception e) {
           log.warn("Error building audit details: {}", e.getMessage());
       }
       // Always return non-null map (empty if errors)
       return details;
   }
   ```

4. **Verify the fix**:
   - Run auditLog_capturesReadOperation test in isolation
   - Check details field is not null
   - Verify details.get("method") returns "readPatient"

Note: Based on verification report, production audit logging works correctly. This is likely a test-only issue with Spring AOP proxy behavior or transaction boundaries.
  </action>
  <verify>
mvn test -Dtest=AuditInterceptorTest#auditLog_capturesReadOperation

Expect:
- Test passes
- Assertion "Details should not be null" succeeds
- details.get("method") equals "readPatient"

Also run full test suite:
mvn test

Expect: 26/26 tests passing (100%)
  </verify>
  <done>auditLog_capturesReadOperation test passes, details field properly populated with method and class information in all test scenarios</done>
</task>

</tasks>

<verification>
**Post-gap-closure verification:**

1. Run full test suite: `mvn test`
   - Expected: 26/26 tests pass (100%)
   - All AuditInterceptorTest cases pass
   - No OptimisticLockingFailureException
   - No null pointer exceptions on details field

2. Verify application still starts: `mvn spring-boot:run`
   - Expected: Application starts successfully
   - Health endpoint returns UP

3. Verify audit logging still works in production:
   - Manual test: Make authenticated request to trigger @Audited method
   - Query database: `docker exec hospital-postgres psql -U hospital_app -d hospital_db -c "SELECT * FROM audit_logs ORDER BY timestamp DESC LIMIT 1;"`
   - Expected: Audit log created with non-null details field

4. Confirm Phase 0 completion criteria:
   - All 9 SEC requirements verified
   - All tests passing
   - No blockers for Phase 1
</verification>

<success_criteria>
1. All 26 security tests pass without failures (was 21/26, now 26/26)
2. AuditInterceptorTest cleanup respects append-only design (no deleteAll() calls)
3. Details field properly populated in all test and production scenarios
4. No OptimisticLockingFailureException in test suite
5. Phase 0 verification gaps closed, ready for Phase 1 patient data implementation
</success_criteria>

<output>
After completion, create `.planning/phases/00-security-compliance-foundation/00-06-SUMMARY.md`

Summary must include:
- Gap closure approach (test infrastructure refactoring)
- Details field investigation findings
- Test results (26/26 passing)
- Phase 0 completion confirmation
- Files modified with specific changes made
</output>
